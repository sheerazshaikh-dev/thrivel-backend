-- Manual fallback for cPanel/phpMyAdmin when the database user cannot ALTER at runtime.
-- MariaDB/MySQL versions that support ADD COLUMN IF NOT EXISTS can run this file safely.
-- On a fresh database, use schema.sql instead.

SET NAMES utf8mb4;

ALTER TABLE products
  ADD COLUMN IF NOT EXISTS compound VARCHAR(255) NULL AFTER description,
  ADD COLUMN IF NOT EXISTS usage_notice VARCHAR(255) NULL AFTER compound,
  ADD COLUMN IF NOT EXISTS use_cases JSON NULL AFTER goal_tags,
  ADD COLUMN IF NOT EXISTS image_url VARCHAR(500) NULL AFTER dosage,
  ADD COLUMN IF NOT EXISTS image_alt VARCHAR(255) NULL AFTER image_url,
  ADD COLUMN IF NOT EXISTS sort_order INT NOT NULL DEFAULT 100;

ALTER TABLE site_settings
  ADD COLUMN IF NOT EXISTS brand_name VARCHAR(190) NOT NULL DEFAULT 'Thrivel IQ',
  ADD COLUMN IF NOT EXISTS tagline VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS logo_dark_url VARCHAR(500) NULL,
  ADD COLUMN IF NOT EXISTS logo_light_url VARCHAR(500) NULL,
  ADD COLUMN IF NOT EXISTS favicon_url VARCHAR(500) NULL,
  ADD COLUMN IF NOT EXISTS hero_image_url VARCHAR(500) NULL,
  ADD COLUMN IF NOT EXISTS auth_image_url VARCHAR(500) NULL,
  ADD COLUMN IF NOT EXISTS checkout_image_url VARCHAR(500) NULL,
  ADD COLUMN IF NOT EXISTS dashboard_image_url VARCHAR(500) NULL,
  ADD COLUMN IF NOT EXISTS assessment_image_url VARCHAR(500) NULL,
  ADD COLUMN IF NOT EXISTS default_product_image_url VARCHAR(500) NULL,
  ADD COLUMN IF NOT EXISTS primary_color VARCHAR(20) NOT NULL DEFAULT '#7AC7C8',
  ADD COLUMN IF NOT EXISTS gradient_mid_color VARCHAR(20) NOT NULL DEFAULT '#9971B1',
  ADD COLUMN IF NOT EXISTS secondary_color VARCHAR(20) NOT NULL DEFAULT '#EC437D',
  ADD COLUMN IF NOT EXISTS accent_color VARCHAR(20) NOT NULL DEFAULT '#F4946E',
  ADD COLUMN IF NOT EXISTS background_color VARCHAR(20) NOT NULL DEFAULT '#0A1133',
  ADD COLUMN IF NOT EXISTS panel_color VARCHAR(20) NOT NULL DEFAULT '#101943',
  ADD COLUMN IF NOT EXISTS support_email VARCHAR(190) NULL,
  ADD COLUMN IF NOT EXISTS footer_text VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS login_headline VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS login_subheadline TEXT NULL,
  ADD COLUMN IF NOT EXISTS login_title VARCHAR(190) NULL,
  ADD COLUMN IF NOT EXISTS login_description VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS signup_headline VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS signup_subheadline TEXT NULL,
  ADD COLUMN IF NOT EXISTS signup_title VARCHAR(190) NULL,
  ADD COLUMN IF NOT EXISTS signup_description VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS account_title VARCHAR(190) NULL,
  ADD COLUMN IF NOT EXISTS account_description VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS checkout_title VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS checkout_description TEXT NULL,
  ADD COLUMN IF NOT EXISTS dashboard_title VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS dashboard_description TEXT NULL;

ALTER TABLE users
  ADD COLUMN IF NOT EXISTS country VARCHAR(100) NULL,
  ADD COLUMN IF NOT EXISTS state VARCHAR(100) NULL,
  ADD COLUMN IF NOT EXISTS phone VARCHAR(40) NULL,
  ADD COLUMN IF NOT EXISTS role ENUM('customer','reviewer','admin') NOT NULL DEFAULT 'customer',
  ADD COLUMN IF NOT EXISTS verified TINYINT(1) NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS plan_updates TINYINT(1) NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS reviewer_messages TINYINT(1) NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS marketing_emails TINYINT(1) NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS api_token_expires_at DATETIME NULL,
  ADD COLUMN IF NOT EXISTS last_login_at DATETIME NULL;

ALTER TABLE orders
  ADD COLUMN IF NOT EXISTS first_name VARCHAR(100) NULL AFTER email,
  ADD COLUMN IF NOT EXISTS last_name VARCHAR(100) NULL AFTER first_name,
  ADD COLUMN IF NOT EXISTS product_subtotal DECIMAL(10,2) NOT NULL DEFAULT 0.00 AFTER stack_price,
  ADD COLUMN IF NOT EXISTS items_json JSON NULL AFTER assessment_json;

ALTER TABLE member_plans
  ADD COLUMN IF NOT EXISTS status VARCHAR(50) NOT NULL DEFAULT 'needs_review',
  ADD COLUMN IF NOT EXISTS focus TEXT NULL,
  ADD COLUMN IF NOT EXISTS nutrition TEXT NULL,
  ADD COLUMN IF NOT EXISTS activity TEXT NULL,
  ADD COLUMN IF NOT EXISTS sleep TEXT NULL,
  ADD COLUMN IF NOT EXISTS recovery TEXT NULL,
  ADD COLUMN IF NOT EXISTS milestones JSON NULL,
  ADD COLUMN IF NOT EXISTS categories JSON NULL,
  ADD COLUMN IF NOT EXISTS product_ids JSON NULL,
  ADD COLUMN IF NOT EXISTS flags JSON NULL,
  ADD COLUMN IF NOT EXISTS reviewer VARCHAR(190) NULL,
  ADD COLUMN IF NOT EXISTS next_check_in DATETIME NULL,
  ADD COLUMN IF NOT EXISTS version INT NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS reviewer_approved_at DATETIME NULL;

ALTER TABLE media
  ADD COLUMN IF NOT EXISTS alt_text VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS size_bytes BIGINT UNSIGNED NOT NULL DEFAULT 0;
